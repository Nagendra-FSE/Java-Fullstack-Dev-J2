CSS


Fonts: Web fonts are fonts loaded from external servers rather than the user's system. When a webpage is accessed, the browser downloads the font from a specified URL, ensuring consistent text appearance across devices, even if the font isn't installed on the user's system.
