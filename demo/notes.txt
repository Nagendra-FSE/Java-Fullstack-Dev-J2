HTML

- Basic structure: `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`.
- Semantic elements: `<header>`, `<footer>`, `<article>`, `<section>`, `<nav>`, etc.
- Forms & validation: `<input>`, `<textarea>`, `<select>`, `pattern`, `required`.
- Accessibility considerations: ARIA attributes, `alt` for images, keyboard navigation.

CSS

- Selectors & specificity (`.class`, `#id`, `element`, `nth-child`).
- Box model: `margin`, `border`, `padding`, `content`.
- Flexbox (`display: flex`, `justify-content`, `align-items`).
- Grid layout (`grid-template-areas`, `grid-row`, `grid-column`).
- Transitions & animations (`@keyframes`, `transform`, `opacity`).

Bootstrap

- Responsive grid system (`col-sm`, `col-md`, `col-lg`).
- Predefined components (buttons, cards, modals, navbar).
- Utility classes (`m-2`, `p-3`, `text-center`, `d-flex`).
- Customization with Sass.

CSS Preprocessors
- Variables (`$primary-color` in SCSS).
- Nested syntax.
- Mixins (`@mixin button-style`).
- Functions & loops.

JavaScript

- ES6+ features (`let`, `const`, arrow functions, destructuring).
- DOM manipulation (`document.querySelector`, `addEventListener`).
- Event delegation.
- Asynchronous JavaScript (`fetch API`, `async/await`).

Advanced JavaScript

- Closures & scopes.
- Higher-order functions (`map`, `filter`, `reduce`).
- Prototype inheritance.
- Modules & bundlers (`import/export` with Webpack).

React

- Components (`function components`/`class components`).
- Props & state management.
- Hooks (`useState`, `useEffect`, `useContext`).
- Lifecycle methods.
- Routing (`React Router`).
- Redux & state management.

Realtime Project
-------------------------


Course duration: 60 days -- daily 1hr class / 30 min session practice/doubts

HTML/CSS ----------- 14 days
Responsive Web Design ------ 3 days
JavaScript ----------------------- 20 days
react ------------------------ 15 days
Realtime Project (React) ---------------- 5 days
Interview Preparation/Tips ---------------- 3 days